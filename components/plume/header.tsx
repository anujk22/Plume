import {Search, ArrowUpRight} from 'lucide-react';
import Link from 'next/link';
import { Brand } from './brand';
export function Header({active='explore',place,onSearch}:{active?:string;place?:string;onSearch?:()=>void}){return <header className="app-header"><Brand/><span className="header-tagline">METHANE OBSERVATIONS,<br/>IN CONTEXT</span>{onSearch&&<button className="header-place" onClick={onSearch}><Search size={21}/><span>{place||'Find a place'}</span><ArrowUpRight size={17}/></button>}<nav aria-label="Main navigation"><Link href="/explore" aria-current={active==='explore'?'page':undefined}>Explore</Link><Link href="/method" aria-current={active==='method'?'page':undefined}>Our method</Link></nav><div className="snapshot-chip"><span className="snapshot-dot"/> Historical snapshot<span>Retrieved 19 Sep 2026</span></div></header>}
